version: "3.9"
services:
    backends:
        image: bukanebi/backends:devs
        ports:
            - 9000-9002:9000
        networks:
            - devnet
        deploy:
            mode: replicated
            replicas: 2

networks:
    devnet:
        external: true
